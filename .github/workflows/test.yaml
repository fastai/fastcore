name: test
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    container: fastai/nbdev
    steps:

    - name: test-docs
      run: conda --help
      
    - name: test-docs
      run: which nbdev_build_docs
